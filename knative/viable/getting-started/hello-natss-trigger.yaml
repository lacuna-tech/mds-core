apiVersion: eventing.knative.dev/v1alpha1
kind: Trigger
metadata:
  name: hello-natss-display
spec:
  broker: natss
  filter:
    attributes:
      type: greeting
  subscriber:
    ref:
     apiVersion: v1
     kind: Service
     name: hello-display
