apiVersion: v1
kind: ConfigMap
metadata:
  name: mds-config-files
  namespace: {{ $.Values.namespace | default "mds" }}
data:
{{ (.Files.Glob (printf "files/mds-config/%s/*.{json,json5}" ( $.Values.Namespace | default "mds" )) ).AsConfig | indent 2 }}
